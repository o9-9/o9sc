name: Submit o9-9.o99
on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  sync-repo:
    name: Sync winget-pkgs repo
    runs-on: ubuntu-latest
    steps:
      - run: gh repo sync o9-9/winget-pkgs -b master
        env:
          GH_TOKEN: ${{ secrets.WINGET_TOKEN }}

  winget:
    name: Publish winget package
    needs: sync-repo
    runs-on: windows-latest
    steps:
      - name: Submit package to Windows Package Manager Community Repository
        run: |
          $packageId = "o9-9.o99"
          $gitToken = "${{ secrets.WINGET_TOKEN }}"

          # Fetching latest release from GitHub
          $github = Invoke-RestMethod -uri "https://api.github.com/repos/o9-9/o99/releases/latest"
          $packageVersion = $github.tag_name.Trim("v")

          # Get the download URL for o99-installer.exe
          $installerUrl = $github.assets |
            Where-Object { $_.name -eq "o99-installer.exe" } |
            Select-Object -ExpandProperty browser_download_url

          # Update package using wingetcreate with URL|architecture format
          Invoke-WebRequest https://aka.ms/wingetcreate/latest -OutFile wingetcreate.exe

          # Create and submit WinGet manifest
          .\wingetcreate.exe new --token $gitToken $installerUrl
